/* SVG rendering/export pipeline for SokoHai Creative Studio. */
const esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=(v,d=0)=>Number.isFinite(+v)?+v:d;
function lines(text,width,fontSize){const max=Math.max(2,Math.floor(width/(fontSize*.57))),words=String(text||'').split(/\s+/),out=[];let line='';words.forEach(w=>{if((line+' '+w).trim().length>max&&line){out.push(line);line=w}else line=(line+' '+w).trim()});if(line)out.push(line);return out.slice(0,12);}
function filterId(l){return `fx_${String(l.id).replace(/[^a-z0-9_]/gi,'')}`}
function defs(c){let s='<defs><linearGradient id="bgGrad" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="'+esc(c.background.color)+'"/><stop offset="1" stop-color="'+esc(c.background.color2||c.background.color)+'"/></linearGradient><pattern id="dots" width="30" height="30" patternUnits="userSpaceOnUse"><circle cx="3" cy="3" r="2" fill="#fff" opacity=".35"/></pattern><pattern id="grid" width="48" height="48" patternUnits="userSpaceOnUse"><path d="M48 0H0V48" fill="none" stroke="#fff" opacity=".25"/></pattern>';
 c.layers.forEach(l=>{const st=l.style||{},f=st.filter||{};s+=`<filter id="${filterId(l)}" x="-30%" y="-30%" width="160%" height="160%"><feColorMatrix type="saturate" values="${num(f.saturation,100)/100}"/><feComponentTransfer><feFuncR type="linear" slope="${num(f.contrast,100)/100}" intercept="${(num(f.brightness,100)-num(f.contrast,100))/200}"/><feFuncG type="linear" slope="${num(f.contrast,100)/100}" intercept="${(num(f.brightness,100)-num(f.contrast,100))/200}"/><feFuncB type="linear" slope="${num(f.contrast,100)/100}" intercept="${(num(f.brightness,100)-num(f.contrast,100))/200}"/></feComponentTransfer><feGaussianBlur stdDeviation="${num(f.blur,0)}"/></filter>`});return s+'</defs>';}
function layerSvg(l){if(l.visible===false)return'';const st=l.style||{},t=`translate(${num(l.x)} ${num(l.y)}) rotate(${num(l.rotation)} ${num(l.width)/2} ${num(l.height)/2}) scale(${st.flipX?-1:1} ${st.flipY?-1:1})`,common=`data-layer-id="${esc(l.id)}" opacity="${num(l.opacity,1)}" transform="${t}" filter="url(#${filterId(l)})"`;
 if(l.type==='text'){const fs=num(st.fontSize,48),lh=fs*num(st.lineHeight,1.1),ls=lines(l.content,l.width,fs),anchor=st.textAlign==='center'?'middle':st.textAlign==='right'?'end':'start',x=anchor==='middle'?l.width/2:anchor==='end'?l.width:0;let tsp='';ls.forEach((line,i)=>tsp+=`<tspan x="${x}" dy="${i?lh:fs}">${esc(st.textTransform==='uppercase'?line.toUpperCase():st.textTransform==='lowercase'?line.toLowerCase():line)}</tspan>`);const shadow=num(st.shadowOpacity)>0?`paint-order="stroke fill" style="filter:drop-shadow(${num(st.shadowX)}px ${num(st.shadowY)}px ${num(st.shadowBlur)}px ${esc(st.shadowColor||'#000')}88)"`:'';return `<g ${common}>${st.backgroundColor&&st.backgroundColor!=='transparent'?`<rect x="-${num(st.padding)}" y="0" width="${num(l.width)+2*num(st.padding)}" height="${num(l.height)}" rx="${num(st.radius)}" fill="${esc(st.backgroundColor)}"/>`:''}<text x="${x}" y="0" text-anchor="${anchor}" fill="${esc(st.fill||'#000')}" font-family="${esc(st.fontFamily||'Inter')}" font-size="${fs}" font-weight="${num(st.fontWeight,400)}" font-style="${esc(st.fontStyle||'normal')}" text-decoration="${esc(st.textDecoration||'none')}" letter-spacing="${num(st.letterSpacing)}" stroke="${esc(st.stroke||'none')}" stroke-width="${num(st.strokeWidth)}" stroke-opacity="${num(st.strokeOpacity,1)}" ${shadow}>${tsp}</text></g>`;}
 if(l.type==='image')return `<g ${common}><clipPath id="clip_${esc(l.id)}"><rect width="${num(l.width)}" height="${num(l.height)}" rx="${num(st.radius)}"/></clipPath><image href="${esc(l.src)}" crossorigin="anonymous" x="${num(st.cropX)}" y="${num(st.cropY)}" width="${num(l.width)*num(st.zoom,1)}" height="${num(l.height)*num(st.zoom,1)}" preserveAspectRatio="${st.fit==='contain'?'xMidYMid meet':'xMidYMid slice'}" clip-path="url(#clip_${esc(l.id)})"/><rect width="${num(l.width)}" height="${num(l.height)}" rx="${num(st.radius)}" fill="none" stroke="${esc(st.borderColor||'none')}" stroke-width="${num(st.borderWidth)}"/></g>`;
 if(l.type==='icon'){const paths={phone:'M7 3l4 4-2 3c2 4 5 7 9 9l3-2 4 4-3 4C12 22 2 12 3 6z',location:'M14 27S6 19 6 11a8 8 0 1 1 16 0c0 8-8 16-8 16zm0-12a4 4 0 1 0 0-8 4 4 0 0 0 0 8z',cart:'M3 4h3l3 14h13l3-9H8m3 14a2 2 0 1 0 0 .1m10-.1a2 2 0 1 0 0 .1',clock:'M14 3a11 11 0 1 0 0 22 11 11 0 0 0 0-22zm0 5v7l5 3',calendar:'M5 7h18v17H5zM9 3v7m10-7v7M5 12h18',verified:'M4 15l6 6L25 6',star:'M14 3l3 8h9l-7 5 3 9-8-5-8 5 3-9-7-5h9z',arrow:'M4 14h21m-7-7 7 7-7 7',chat:'M4 5h21v15H12l-7 5V5z',delivery:'M3 8h14v12H3zM17 12h6l4 5v3H17M8 23a2 2 0 1 0 0 .1m14-.1a2 2 0 1 0 0 .1',price:'M4 5h14l8 9-12 12L4 16zM9 10a2 2 0 1 0 0 .1',discount:'M6 23L22 5M8 6a3 3 0 1 0 0 .1m12 14a3 3 0 1 0 0 .1'};const p=paths[l.icon]||paths.star,scale=Math.min(num(l.width),num(l.height))/30;return `<g ${common}><circle cx="${num(l.width)/2}" cy="${num(l.height)/2}" r="${Math.min(num(l.width),num(l.height))/2}" fill="${esc(st.backgroundColor||'transparent')}"/><path d="${p}" transform="translate(${(num(l.width)-30*scale)/2} ${(num(l.height)-30*scale)/2}) scale(${scale})" fill="${['star','price'].includes(l.icon)?esc(st.fill||'#fff'):'none'}" stroke="${esc(st.fill||'#fff')}" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></g>`;}
 const shape=l.shape||'rectangle',fill=esc(st.fill||'#fff'),stroke=esc(st.borderColor||'none'),sw=num(st.borderWidth);if(shape==='circle')return `<ellipse ${common} cx="${num(l.width)/2}" cy="${num(l.height)/2}" rx="${num(l.width)/2}" ry="${num(l.height)/2}" fill="${fill}" stroke="${stroke}" stroke-width="${sw}"/>`;if(shape==='line'||shape==='arrow')return `<g ${common}><line x1="0" y1="${num(l.height)/2}" x2="${num(l.width)}" y2="${num(l.height)/2}" stroke="${fill}" stroke-width="${Math.max(2,sw||8)}"/>${shape==='arrow'?`<path d="M${num(l.width)-25} ${num(l.height)/2-20}L${num(l.width)} ${num(l.height)/2}L${num(l.width)-25} ${num(l.height)/2+20}" fill="none" stroke="${fill}" stroke-width="${Math.max(2,sw||8)}"/>`:''}</g>`;return `<rect ${common} width="${num(l.width)}" height="${num(l.height)}" rx="${num(st.radius)}" fill="${fill}" stroke="${stroke}" stroke-width="${sw}"/>`;}
export function renderCreativeSvg(c,{guides=false,selectedId=''}={}){const w=c.canvas.width,h=c.canvas.height,bg=c.background||{},sorted=[...c.layers].sort((a,b)=>a.zIndex-b.zIndex);let background=bg.type==='image'&&bg.imageUrl?`<image href="${esc(bg.imageUrl)}" crossorigin="anonymous" width="${w}" height="${h}" preserveAspectRatio="xMidYMid slice"/>`:`<rect width="${w}" height="${h}" fill="${bg.type==='gradient'?'url(#bgGrad)':esc(bg.color||'#fff')}"/>`;if(bg.texture&&bg.texture!=='none')background+=`<rect width="${w}" height="${h}" fill="url(#${bg.texture==='grid'?'grid':'dots'})" opacity="${num(bg.textureOpacity,.08)}"/>`;const guide=guides?`<rect x="${c.canvas.safe}" y="${c.canvas.safe}" width="${w-2*c.canvas.safe}" height="${h-2*c.canvas.safe}" fill="none" stroke="#22D3EE" stroke-width="3" stroke-dasharray="14 12" pointer-events="none"/><path d="M${w/2} 0V${h}M0 ${h/2}H${w}" stroke="#22D3EE" stroke-width="2" opacity=".55" pointer-events="none"/>`:'';const sel=selectedId?(()=>{const l=c.layers.find(x=>x.id===selectedId);if(!l)return'';const x=l.x,y=l.y,w=l.width,h=l.height;return `<g class="cs-selection"><rect x="${x}" y="${y}" width="${w}" height="${h}" fill="none" stroke="#38BDF8" stroke-width="4" stroke-dasharray="12 8" pointer-events="none"/><circle data-resize-id="${esc(l.id)}" data-corner="se" cx="${x+w}" cy="${y+h}" r="15" fill="#fff" stroke="#0284C7" stroke-width="6" style="cursor:nwse-resize"/><circle data-resize-id="${esc(l.id)}" data-corner="ne" cx="${x+w}" cy="${y}" r="13" fill="#fff" stroke="#0284C7" stroke-width="5" style="cursor:nesw-resize"/></g>`})():'';return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${w} ${h}" width="100%" height="100%" role="img" aria-label="${esc(c.title||'Creative design')}">${defs(c)}${background}${sorted.map(layerSvg).join('')}${guide}${sel}</svg>`;}
export async function exportCreative(c,type='png',quality=.92){const svg=renderCreativeSvg(c),blob=new Blob([svg],{type:'image/svg+xml;charset=utf-8'}),url=URL.createObjectURL(blob);try{const img=new Image();img.crossOrigin='anonymous';await new Promise((ok,bad)=>{img.onload=ok;img.onerror=()=>bad(new Error('One or more images could not be loaded for export. Check Cloudinary/CORS access.'));img.src=url});const canvas=document.createElement('canvas');canvas.width=c.canvas.width;canvas.height=c.canvas.height;const ctx=canvas.getContext('2d');if(type==='jpg'||type==='jpeg'){ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height)}ctx.drawImage(img,0,0);return await new Promise((ok,bad)=>canvas.toBlob(b=>b?ok(b):bad(new Error('Export failed.')),type==='jpg'||type==='jpeg'?'image/jpeg':'image/png',quality));}finally{URL.revokeObjectURL(url)}}
export function downloadBlob(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1000);}
