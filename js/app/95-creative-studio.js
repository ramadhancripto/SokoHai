/* SokoHai Creative Studio — one lazy-loaded editor for every source/format. */
import { skh } from './00-bootstrap.js';
import {createCreative,normalizeCreative,duplicateCreative,resizeCreative,applyEntity,makeLayer,FORMAT_PRESETS,VERIFIED_FONTS,templatesFor,autoDesignVariations,designSuggestions,validateCreative} from './creative/creative-model.js';
import {CreativeHistory} from './creative/creative-history.js';
import {renderCreativeSvg,exportCreative,downloadBlob} from './creative/creative-svg-renderer.js';

const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)], esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let state=null,history=null,selected='',activeTab='templates',saveTimer=null,drag=null,zoom=.58,guides=true,variations=[];
const storageKey=c=>'skh_creative_draft_'+(c.id||'new_'+(c.linkedEntity?.id||'blank'));
function toast(m,t='success'){if(window.skhToast)window.skhToast(m,t);else alert(m)}
function ensureCss(){if($('#skhCreativeCss'))return;const l=document.createElement('link');l.id='skhCreativeCss';l.rel='stylesheet';l.href='css/39-creative-studio.css';document.head.appendChild(l)}
function currentLayer(){return state?.layers.find(l=>l.id===selected)||null}
function commit(mutator,label){const next=JSON.parse(JSON.stringify(state));mutator(next);next.updatedAt=new Date().toISOString();state=history.commit(next);render(label);scheduleSave()}
function replace(next){state=history.replace(normalizeCreative(next));selected='';render();scheduleSave()}
function scheduleSave(){clearTimeout(saveTimer);saveTimer=setTimeout(()=>saveDraft(false),900)}
function shell(){let m=$('#skhCreativeStudio');if(m)return m;m=document.createElement('div');m.id='skhCreativeStudio';m.className='skh-cs';m.setAttribute('role','dialog');m.setAttribute('aria-modal','true');m.innerHTML=`
 <header class="skh-cs-top"><button data-a="close" aria-label="Close">×</button><div class="skh-cs-brand"><b>SokoHai Creative Studio</b><input id="csTitle" aria-label="Design title"></div><div class="skh-cs-tools"><button data-a="undo" title="Undo">↶</button><button data-a="redo" title="Redo">↷</button><button data-a="preview">Preview</button><button class="cs-mobile-props" data-a="mobileprops">Properties</button><button data-a="save">Save</button><button data-a="duplicate">Duplicate</button><button data-a="resize">Resize</button><button class="accent" data-a="auto">Auto Design</button><button data-a="export">Export</button><button class="primary" data-a="publish">Publish</button></div></header>
 <main class="skh-cs-layout"><aside class="skh-cs-left"><nav>${['templates','elements','uploads','brand','background','text'].map(x=>`<button data-tab="${x}">${x}</button>`).join('')}</nav><div id="csLibrary" class="skh-cs-library"></div></aside><section class="skh-cs-work"><div class="skh-cs-status"><span id="csFormat"></span><span id="csSaveState">Draft</span></div><div id="csStage" class="skh-cs-stage"><div id="csCanvas" class="skh-cs-canvas"></div></div><footer><label>Zoom <input id="csZoom" type="range" min="20" max="100" value="58"></label><button data-a="guides">Guides</button><button data-a="grid">Grid</button><button data-a="fit">Fit</button></footer></section><aside class="skh-cs-right"><div class="skh-cs-right-head"><b>Properties</b><button data-a="layers">Layers</button></div><div id="csProperties"></div><div id="csAssistant"></div></aside></main>
 <div id="csSheet" class="skh-cs-sheet" hidden></div>`;document.body.appendChild(m);bind(m);return m}
function bind(m){m.addEventListener('click',e=>{const a=e.target.closest('[data-a]')?.dataset.a,tab=e.target.closest('[data-tab]')?.dataset.tab;if(tab){activeTab=tab;renderLibrary();return}if(a)action(a,e);const propBtn=e.target.closest('button[data-prop]');if(propBtn)updateProp(propBtn.dataset.prop,propBtn);const tpl=e.target.closest('[data-template]')?.dataset.template;if(tpl){const t=templatesFor(state.format).find(x=>x.id===tpl);if(t)replace({...t.creative,linkedEntity:state.linkedEntity,destination:state.destination,id:state.id,ownerId:state.ownerId})}const add=e.target.closest('[data-add]')?.dataset.add;if(add)addLayer(add,e.target.closest('[data-add]').dataset.value);const ly=e.target.closest('[data-layer]')?.dataset.layer;if(ly){selected=ly;renderCanvas();renderProperties()}const vari=e.target.closest('[data-variation]')?.dataset.variation;if(vari&&variations[+vari])replace(variations[+vari].creative)});
 m.addEventListener('input',e=>{if(e.target.id==='csZoom'){zoom=+e.target.value/100;scaleCanvas();return}if(e.target.id==='csTitle'){state.title=e.target.value;scheduleSave();return}const key=e.target.dataset.prop;if(key)updateProp(key,e.target)});
 m.addEventListener('change',e=>{if(e.target.dataset.bg)updateBackground(e.target.dataset.bg,e.target);if(e.target.id==='csImageFile')uploadImage(e.target)});
 $('#csCanvas',m).addEventListener('pointerdown',pointerStart);window.addEventListener('pointermove',pointerMove);window.addEventListener('pointerup',pointerEnd);
 document.addEventListener('keydown',e=>{if(!$('#skhCreativeStudio')?.classList.contains('open'))return;if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'){e.preventDefault();state=e.shiftKey?history.redo():history.undo();render()}else if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='y'){e.preventDefault();state=history.redo();render()}else if((e.key==='Delete'||e.key==='Backspace')&&selected&&!/INPUT|TEXTAREA/.test(e.target.tagName)){commit(n=>n.layers=n.layers.filter(l=>l.id!==selected));selected=''}})}
function action(a){if(a==='close'){close();return}if(a==='mobileprops'){$('.skh-cs-right')?.classList.toggle('mobile-open');return}if(a==='undo'){state=history.undo();render();return}if(a==='redo'){state=history.redo();render();return}if(a==='guides'){guides=!guides;renderCanvas();return}if(a==='fit'){zoom=Math.min(.8,Math.max(.2,($('#csStage').clientHeight-80)/state.canvas.height));$('#csZoom').value=Math.round(zoom*100);scaleCanvas();return}if(a==='save'){saveDraft(true);return}if(a==='duplicate'){replace(duplicateCreative(state));toast('Creative duplicated.');return}if(a==='auto'){variations=autoDesignVariations(state);activeTab='templates';renderLibrary(true);return}if(a==='preview'){showPreview();return}if(a==='export'){showExport();return}if(a==='resize'){showResize();return}if(a==='publish'){publish();return}if(a==='layers'){showLayers();return}if(a==='grid'){guides=true;renderCanvas();return}}
function render(){if(!state)return;$('#csTitle').value=state.title;$('#csFormat').textContent=`${state.format} · ${state.canvas.width} × ${state.canvas.height}`;renderCanvas();renderLibrary();renderProperties();renderAssistant();const u=$('[data-a="undo"]'),r=$('[data-a="redo"]');if(u)u.disabled=!history.canUndo;if(r)r.disabled=!history.canRedo}
function renderCanvas(){const box=$('#csCanvas');if(!box)return;box.innerHTML=renderCreativeSvg(state,{guides,selectedId:selected});scaleCanvas();$$('[data-layer-id]',box).forEach(n=>{n.style.cursor='move';n.setAttribute('tabindex','0')})}
function scaleCanvas(){const b=$('#csCanvas');if(!b||!state)return;b.style.width=(state.canvas.width*zoom)+'px';b.style.height=(state.canvas.height*zoom)+'px'}
function renderLibrary(showVars=false){const box=$('#csLibrary');if(!box)return;$$('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===activeTab));if(showVars||variations.length&&activeTab==='templates')box.innerHTML='<h3>Auto Design Variations</h3><p>Choose a labeled layout. None is called “best”.</p><div class="cs-templates">'+variations.map((v,i)=>`<button data-variation="${i}" style="--a:${v.creative.background.color};--b:${v.creative.background.color2}"><i></i><b>${esc(v.name)}</b></button>`).join('')+'</div><hr><h3>Templates</h3>'+templateCards();else if(activeTab==='templates')box.innerHTML='<h3>Editable Templates</h3><p>Every element remains a layer.</p>'+templateCards();else if(activeTab==='elements')box.innerHTML=`<h3>Elements</h3><div class="cs-addgrid"><button data-add="shape" data-value="rectangle">Rectangle</button><button data-add="shape" data-value="circle">Circle</button><button data-add="shape" data-value="line">Line</button><button data-add="shape" data-value="arrow">Arrow</button><button data-add="shape" data-value="badge">Badge</button>${['phone','location','chat','cart','delivery','clock','calendar','price','discount','verified','star','arrow'].map(i=>`<button data-add="icon" data-value="${i}">${i}</button>`).join('')}</div>`;else if(activeTab==='text')box.innerHTML=`<h3>Typography</h3><button class="cs-wide" data-add="text" data-value="headline">Add headline</button><button class="cs-wide" data-add="text" data-value="subheadline">Add subheadline</button><button class="cs-wide" data-add="text" data-value="body">Add body</button><button class="cs-wide" data-add="text" data-value="price">Add price</button><button class="cs-wide" data-add="text" data-value="cta">Add CTA</button><button class="cs-wide" data-add="auto-typography">Auto Typography</button><p class="cs-note">Verified fonts: ${VERIFIED_FONTS.map(f=>f.family).join(', ')}. Unverified fonts are intentionally unavailable.</p>`;else if(activeTab==='uploads')box.innerHTML=`<h3>Uploads</h3><label class="cs-upload">Upload image<input id="csImageFile" type="file" accept="image/jpeg,image/png,image/webp" hidden></label><button class="cs-wide" data-add="image-url">Use HTTPS image URL</button><p>Uses the existing SokoHai Cloudinary uploader. Originals are not stored in Firestore.</p>`;else if(activeTab==='background')box.innerHTML=backgroundControls();else if(activeTab==='brand')box.innerHTML=brandControls()}
function templateCards(){return '<div class="cs-templates">'+templatesFor(state.format).map(t=>`<button data-template="${t.id}" style="--a:${t.preview.a};--b:${t.preview.b}"><i></i><b>${esc(t.name)}</b></button>`).join('')+'</div>'}
function backgroundControls(){const b=state.background;return `<h3>Background</h3><label>Type<select data-bg="type"><option ${b.type==='solid'?'selected':''} value="solid">Solid</option><option ${b.type==='gradient'?'selected':''} value="gradient">Gradient</option><option ${b.type==='image'?'selected':''} value="image">Image</option></select></label><label>Primary<input data-bg="color" type="color" value="${b.color}"></label><label>Secondary<input data-bg="color2" type="color" value="${b.color2}"></label><label>Angle<input data-bg="angle" type="range" min="0" max="360" value="${b.angle}"></label><label>Image URL<input data-bg="imageUrl" value="${esc(b.imageUrl||'')}" placeholder="https://..."></label><label>Texture<select data-bg="texture"><option>none</option><option ${b.texture==='dots'?'selected':''}>dots</option><option ${b.texture==='grid'?'selected':''}>grid</option></select></label><label>Texture opacity<input data-bg="textureOpacity" type="range" min="0" max="0.35" step="0.01" value="${b.textureOpacity}"></label>`}
function brandControls(){const b=state.brandKit||{};return `<h3>Brand Kit</h3><label>Business name<input data-bg="brand.name" value="${esc(b.name||'')}"></label><label>Logo URL<input data-bg="brand.logoUrl" value="${esc(b.logoUrl||'')}"></label><label>Primary<input data-bg="brand.primary" type="color" value="${b.primary||'#0E7A5F'}"></label><label>Secondary<input data-bg="brand.secondary" type="color" value="${b.secondary||'#167A91'}"></label><label>Accent<input data-bg="brand.accent" type="color" value="${b.accent||'#F4C542'}"></label><button class="cs-wide" data-add="save-brand">Save Brand Kit</button><button class="cs-wide" data-add="apply-brand">Apply Brand Kit</button><p>Brand changes are applied only when you choose Apply.</p>`}
function addLayer(type,value){
 if(type==='duplicate-layer'){const l=currentLayer();if(l)commit(n=>{const copy=JSON.parse(JSON.stringify(l));copy.id='ly_'+Date.now().toString(36);copy.x+=24;copy.y+=24;copy.zIndex=Math.max(...n.layers.map(x=>x.zIndex))+1;n.layers.push(copy);selected=copy.id});return}
 if(type==='delete-layer'){if(selected)commit(n=>n.layers=n.layers.filter(l=>l.id!==selected));selected='';return}
 if(type.startsWith('align-')){const l=currentLayer(),a=type.slice(6);if(!l)return;commit(n=>{const x=n.layers.find(q=>q.id===selected),w=n.canvas.width,h=n.canvas.height,s=n.canvas.safe;if(a==='left')x.x=s;if(a==='center')x.x=(w-x.width)/2;if(a==='right')x.x=w-s-x.width;if(a==='top')x.y=s;if(a==='middle')x.y=(h-x.height)/2;if(a==='bottom')x.y=h-s-x.height});return}
 if(type==='image-url'){const src=prompt('HTTPS image URL');if(src&&/^https:\/\//i.test(src))commit(n=>n.layers.push(makeLayer('image',{src,name:'Image',zIndex:n.layers.length+1})));return}
 if(type==='auto-typography'){commit(n=>{const base=n.canvas.width/1080;n.layers.filter(l=>l.type==='text').forEach(l=>{const sizes={headline:82,subheadline:52,price:68,cta:32,body:34};l.style.fontSize=Math.round((sizes[l.role]||34)*base);l.style.fontWeight=['headline','price','cta'].includes(l.role)?900:l.role==='subheadline'?700:500;l.style.lineHeight=l.role==='headline'?1.02:1.2})});return}
 if(type==='save-brand'){saveBrandKit();return}
 if(type==='apply-brand'){const b=state.brandKit;if(!b)return toast('Add Brand Kit values first.','error');commit(n=>{n.background.color=b.primary||n.background.color;n.background.color2=b.secondary||n.background.color2;n.layers.filter(l=>l.role==='cta-bg').forEach(l=>l.style.fill=b.accent||l.style.fill);if(b.logoUrl)n.layers.push(makeLayer('image',{name:'Brand logo',src:b.logoUrl,x:70,y:70,width:170,height:100,zIndex:n.layers.length+1,style:{fit:'contain'}}))});return}
 const z=Math.max(0,...state.layers.map(l=>l.zIndex))+1;
 if(type==='text'){const content=value==='headline'?'YOUR HEADLINE':value==='price'?'TZS 0':value==='cta'?'TAZAMA ZAIDI':value==='subheadline'?'Subheadline':'Add your message';const fs=value==='headline'?72:value==='price'?64:value==='cta'?36:42;commit(n=>{const l=makeLayer('text',{content,role:value,x:100,y:150,width:700,height:150,zIndex:z,style:{fontSize:fs,fontWeight:value==='headline'||value==='price'?900:600,fill:'#FFFFFF'}});n.layers.push(l);selected=l.id})}
 else if(type==='shape'){commit(n=>{const l=makeLayer('shape',{shape:value,x:180,y:220,width:420,height:value==='line'||value==='arrow'?50:260,zIndex:z,style:{fill:'#F4C542',radius:value==='badge'?999:24}});n.layers.push(l);selected=l.id})}
 else if(type==='icon'){commit(n=>{const l=makeLayer('icon',{icon:value,x:180,y:220,width:110,height:110,zIndex:z,style:{fill:'#FFFFFF',backgroundColor:'#0E7A5F'}});n.layers.push(l);selected=l.id})}
}
async function uploadImage(input){const file=input.files?.[0];if(!file)return;input.disabled=true;try{if(typeof window.skhUploadFromFile!=='function')throw new Error('SokoHai media uploader is unavailable.');const uploaded=await window.skhUploadFromFile(file,{folder:'sokohai/creative-assets'}),url=typeof uploaded==='string'?uploaded:uploaded&&uploaded.url;if(!url)throw new Error('Upload returned no URL.');commit(n=>{const l=makeLayer('image',{src:url,name:file.name,x:120,y:180,width:700,height:600,zIndex:n.layers.length+1,style:{fit:'cover',radius:22}});n.layers.push(l);selected=l.id});toast('Image uploaded to the existing media service.')}catch(e){toast(e.message,'error')}finally{input.disabled=false;input.value=''}}
function renderProperties(){const box=$('#csProperties');if(!box)return;const l=currentLayer();if(!l){box.innerHTML='<div class="cs-empty"><b>Select an element</b><p>Click a layer on the canvas or open Layers.</p></div>';return}const st=l.style||{};box.innerHTML=`<div class="cs-prop-head"><input data-prop="name" value="${esc(l.name)}"><button data-add="duplicate-layer">Duplicate</button><button data-add="delete-layer">Delete</button></div>${l.type==='text'?`<label>Text<textarea data-prop="content" rows="4">${esc(l.content)}</textarea></label><label>Font<select data-prop="style.fontFamily"><option>Inter</option></select></label><div class="cs-two"><label>Size<input data-prop="style.fontSize" type="number" value="${st.fontSize}"></label><label>Weight<select data-prop="style.fontWeight">${[400,500,600,700,800,900].map(v=>`<option ${+st.fontWeight===v?'selected':''}>${v}</option>`).join('')}</select></label></div><label>Color<input data-prop="style.fill" type="color" value="${st.fill||'#000000'}"></label><div class="cs-three"><button data-prop="style.fontStyle" value="${st.fontStyle==='italic'?'normal':'italic'}">Italic</button><button data-prop="style.textDecoration" value="${st.textDecoration==='underline'?'none':'underline'}">Underline</button><select data-prop="style.textAlign"><option>left</option><option ${st.textAlign==='center'?'selected':''}>center</option><option ${st.textAlign==='right'?'selected':''}>right</option></select></div><label>Line height<input data-prop="style.lineHeight" type="range" min="0.8" max="2" step="0.05" value="${st.lineHeight}"></label><label>Letter spacing<input data-prop="style.letterSpacing" type="range" min="-4" max="24" value="${st.letterSpacing}"></label><fieldset><legend>Stroke</legend><label>Width<input data-prop="style.strokeWidth" type="range" min="0" max="10" step=".5" value="${st.strokeWidth}"></label><label>Color<input data-prop="style.stroke" type="color" value="${st.stroke||'#FFFFFF'}"></label></fieldset><fieldset><legend>Shadow</legend><label>Opacity<input data-prop="style.shadowOpacity" type="range" min="0" max="1" step=".05" value="${st.shadowOpacity}"></label><label>Blur<input data-prop="style.shadowBlur" type="range" min="0" max="40" value="${st.shadowBlur}"></label></fieldset>`:''}${l.type==='image'?`<label>Image URL<input data-prop="src" value="${esc(l.src||'')}"></label><label>Fit<select data-prop="style.fit"><option>cover</option><option ${st.fit==='contain'?'selected':''}>contain</option></select></label><label>Brightness<input data-prop="style.filter.brightness" type="range" min="20" max="180" value="${st.filter?.brightness||100}"></label><label>Contrast<input data-prop="style.filter.contrast" type="range" min="20" max="180" value="${st.filter?.contrast||100}"></label><label>Saturation<input data-prop="style.filter.saturation" type="range" min="0" max="200" value="${st.filter?.saturation||100}"></label><label>Blur<input data-prop="style.filter.blur" type="range" min="0" max="20" value="${st.filter?.blur||0}"></label>`:''}${l.type!=='text'&&l.type!=='image'?`<label>Fill<input data-prop="style.fill" type="color" value="${st.fill||'#FFFFFF'}"></label><label>Radius<input data-prop="style.radius" type="range" min="0" max="300" value="${st.radius||0}"></label>`:''}<fieldset><legend>Position</legend><div class="cs-two"><label>X<input data-prop="x" type="number" value="${Math.round(l.x)}"></label><label>Y<input data-prop="y" type="number" value="${Math.round(l.y)}"></label><label>Width<input data-prop="width" type="number" value="${Math.round(l.width)}"></label><label>Height<input data-prop="height" type="number" value="${Math.round(l.height)}"></label></div><label>Rotation<input data-prop="rotation" type="range" min="-180" max="180" value="${l.rotation}"></label><label>Opacity<input data-prop="opacity" type="range" min="0" max="1" step=".05" value="${l.opacity}"></label></fieldset><div class="cs-align">${['left','center','right','top','middle','bottom'].map(x=>`<button data-add="align-${x}">${x}</button>`).join('')}</div>`}
function updateProp(path,input){const value=input.type==='number'||input.type==='range'?+input.value:input.value;if(input.tagName==='BUTTON')input.value=value;commit(n=>{const l=n.layers.find(x=>x.id===selected);if(!l)return;const p=path.split('.');let o=l;while(p.length>1){const k=p.shift();o[k]=o[k]||{};o=o[k]}o[p[0]]=value})}
function updateBackground(path,input){const val=input.type==='range'?+input.value:input.value;commit(n=>{if(path.startsWith('brand.')){n.brandKit=n.brandKit||{};n.brandKit[path.split('.')[1]]=val}else n.background[path]=val})}
function renderAssistant(){const out=designSuggestions(state),box=$('#csAssistant');box.innerHTML='<h3>Design Assistant</h3>'+(out.length?out.map(x=>`<button data-layer="${x.layerId||''}" class="${x.level}">${esc(x.message)}</button>`).join(''):'<p class="ok">No obvious layout warning found.</p>')}
function pointerStart(e){const handle=e.target.closest('[data-resize-id]'),el=e.target.closest('[data-layer-id]');const id=handle?.dataset.resizeId||el?.dataset.layerId;if(!id)return;const l=state.layers.find(x=>x.id===id);if(!l||l.locked)return;e.preventDefault();selected=l.id;drag={id:l.id,mode:handle?'resize':'move',corner:handle?.dataset.corner,startX:e.clientX,startY:e.clientY,x:l.x,y:l.y,width:l.width,height:l.height,group:l.groupId?state.layers.filter(x=>x.groupId===l.groupId).map(x=>({id:x.id,x:x.x,y:x.y})):[],before:JSON.parse(JSON.stringify(state))};renderProperties();e.target.setPointerCapture?.(e.pointerId)}
function pointerMove(e){if(!drag)return;const l=state.layers.find(x=>x.id===drag.id);if(!l)return;const snap=10,dx=(e.clientX-drag.startX)/zoom,dy=(e.clientY-drag.startY)/zoom;if(drag.mode==='resize'){l.width=Math.max(20,Math.round((drag.width+dx)/snap)*snap);l.height=Math.max(20,Math.round((drag.height+dy*(drag.corner==='ne'?-1:1))/snap)*snap);if(drag.corner==='ne')l.y=drag.y+drag.height-l.height}else{l.x=Math.round((drag.x+dx)/snap)*snap;l.y=Math.round((drag.y+dy)/snap)*snap;drag.group.forEach(o=>{const q=state.layers.find(x=>x.id===o.id);if(q&&q.id!==l.id){q.x=Math.round((o.x+dx)/snap)*snap;q.y=Math.round((o.y+dy)/snap)*snap}})}renderCanvas()}
function pointerEnd(){if(!drag)return;const next=JSON.parse(JSON.stringify(state));history.replace(drag.before);state=history.commit(next);drag=null;render();scheduleSave()}
function sheet(html){const s=$('#csSheet');s.hidden=false;s.innerHTML=`<div class="skh-cs-sheet-card"><button class="cs-sheet-close">×</button>${html}</div>`;$('.cs-sheet-close',s).onclick=()=>s.hidden=true;return s}
function showLayers(){const s=sheet('<h2>Layers</h2><p>Select two or more layers to group them.</p><div><button data-group="group">Group selected</button> <button data-group="ungroup">Ungroup selected</button></div><div class="cs-layer-list">'+[...state.layers].sort((a,b)=>b.zIndex-a.zIndex).map(l=>`<article><input type="checkbox" data-pick="${l.id}" aria-label="Select ${esc(l.name)}"><button data-layer="${l.id}">${l.visible===false?'○':'●'} ${esc(l.name)}${l.groupId?' · grouped':''}</button><button data-op="up" data-id="${l.id}">↑</button><button data-op="down" data-id="${l.id}">↓</button><button data-op="lock" data-id="${l.id}">${l.locked?'Unlock':'Lock'}</button><button data-op="hide" data-id="${l.id}">${l.visible===false?'Show':'Hide'}</button></article>`).join('')+'</div>');s.onclick=e=>{const grouping=e.target.dataset.group;if(grouping){const ids=$$('[data-pick]:checked',s).map(x=>x.dataset.pick);if(!ids.length)return;commit(n=>{const gid=grouping==='group'?'grp_'+Date.now().toString(36):null;n.layers.filter(l=>ids.includes(l.id)).forEach(l=>l.groupId=gid)});showLayers();return}const id=e.target.dataset.id,op=e.target.dataset.op;if(!id||!op)return;commit(n=>{const l=n.layers.find(x=>x.id===id);if(op==='up')l.zIndex++;if(op==='down')l.zIndex--;if(op==='lock')l.locked=!l.locked;if(op==='hide')l.visible=!l.visible});showLayers()}}
function showResize(){const opts=Object.entries(FORMAT_PRESETS).map(([k,v])=>`<option value="${k}">${v.label} — ${v.width}×${v.height}</option>`).join('');const s=sheet(`<h2>Resize Creative</h2><p>Elements are proportionally adapted; major ratio changes reflow semantic layers for review.</p><label>Preset<select id="csResizePreset">${opts}<option value="custom">Custom</option></select></label><div class="cs-two"><label>Width<input id="csResizeW" type="number" value="${state.canvas.width}"></label><label>Height<input id="csResizeH" type="number" value="${state.canvas.height}"></label></div><button id="csDoResize" class="primary">Resize and review</button>`);$('#csResizePreset',s).onchange=e=>{const p=FORMAT_PRESETS[e.target.value];if(p){$('#csResizeW',s).value=p.width;$('#csResizeH',s).value=p.height}};$('#csDoResize',s).onclick=()=>{replace(resizeCreative(state,$('#csResizePreset',s).value,+$('#csResizeW',s).value,+$('#csResizeH',s).value));s.hidden=true}}
function showPreview(){sheet(`<h2>Preview modes</h2><div class="cs-previews"><article><b>Feed</b><div>${renderCreativeSvg(state)}</div></article><article class="phone"><b>Mobile / Story</b><div>${renderCreativeSvg(state)}</div></article><article class="desktop"><b>Desktop</b><div>${renderCreativeSvg(state)}</div></article></div>`)}
function showExport(){const s=sheet(`<h2>Export</h2><p>Editable JSON remains saved separately. Export creates a flattened delivery image only.</p><button id="csPng">Download PNG</button><button id="csJpg">Download JPG</button>`);const run=async type=>{try{const b=await exportCreative(state,type);downloadBlob(b,(state.title||'creative').replace(/\W+/g,'-')+'.'+type);toast(type.toUpperCase()+' exported.')}catch(e){toast(e.message,'error')}};$('#csPng',s).onclick=()=>run('png');$('#csJpg',s).onclick=()=>run('jpg')}
async function saveBrandKit(){if(!state.brandKit||!state.brandKit.name)return toast('Enter a business name before saving Brand Kit.','error');localStorage.setItem('skh_brand_kit',JSON.stringify(state.brandKit));if(!skh.currentUser)return toast('Brand Kit saved on this device. Sign in for cloud save.','warning');try{await skh.setDoc(skh.doc(skh.db,'brandKits',skh.currentUser.uid+'_default'),{...state.brandKit,ownerId:skh.currentUser.uid,updatedAt:new Date().toISOString()},{merge:true});toast('Brand Kit saved.')}catch(e){toast('Brand Kit cloud save failed: '+e.message,'error')}}
async function saveDraft(notify){localStorage.setItem(storageKey(state),JSON.stringify(state));$('#csSaveState').textContent='Saved locally';if(skh.currentUser){try{const payload={...state,ownerId:skh.currentUser.uid,status:state.status||'DRAFT',updatedAt:new Date().toISOString()};if(state.id)await skh.setDoc(skh.doc(skh.db,'creatives',state.id),payload,{merge:true});else{payload.createdAt=payload.createdAt||new Date().toISOString();const ref=await skh.addDoc(skh.collection(skh.db,'creatives'),payload);state.id=ref.id;history.replace(state)}$('#csSaveState').textContent='Saved';if(notify)toast('Editable Creative saved.')}catch(e){$('#csSaveState').textContent='Local save only';if(notify)toast('Cloud save failed: '+e.message,'error')}}else if(notify)toast('Saved on this device. Sign in for cloud save.','warning')}
async function publish(){const check=validateCreative(state,{forPublish:true});if(!check.ok){sheet('<h2>Cannot publish yet</h2><ul class="cs-errors">'+check.errors.map(e=>`<li>${esc(e.message)}</li>`).join('')+'</ul>');return}await saveDraft(false);if(!skh.currentUser)return toast('Sign in before publishing.','error');const s=sheet(`<h2>Final pre-publish check</h2><ul class="cs-check"><li>✓ Image and layers loaded</li><li>✓ No technical overflow</li><li>✓ Destination exists in design data</li><li>✓ Linked entity selected</li><li>✓ Creative format valid</li></ul><label>Publication type<select id="csPubType"><option value="advertisement">Advertisement</option><option value="business_update">Business Update</option><option value="product_post">Product Post</option><option value="service_post">Service Post</option><option value="general_post">General Post</option></select></label><p>Non-admin advertisements enter moderation and cannot appear Home until approved.</p><button id="csConfirmPublish" class="primary">Publish pinned version</button>`);$('#csConfirmPublish',s).onclick=async()=>{try{$('#csConfirmPublish',s).disabled=true;const result=await skh.callFunction('creativePublish',{creativeId:state.id,publicationType:$('#csPubType',s).value});const d=result.data||result;state.status=d.status||'READY';state.version=d.version||state.version;history.replace(state);s.hidden=true;render();toast(d.moderationStatus==='pending'?'Submitted for moderation.':'Creative published.')}catch(e){toast('Publish failed: '+(e.message||e),'error');$('#csConfirmPublish',s).disabled=false}}}
async function fetchEntity(context){if(!context.sourceId||!context.sourceType||context.sourceType==='custom')return null;const map={product:'products',service:'services',transport:'delivery',seller:'publicProfiles',business:'publicProfiles'};const col=map[context.sourceType];if(!col)return null;try{const snap=await skh.getDoc(skh.doc(skh.db,col,context.sourceId));return snap.exists()?{id:snap.id,...snap.data()}:null}catch(e){toast('Source could not be loaded: '+e.message,'error');return null}}
async function open(context={}){ensureCss();const m=shell();let c=context.creative?normalizeCreative(context.creative):createCreative({...context,ownerId:skh.currentUser?.uid||''});if(context.creativeId){try{const snap=await skh.getDoc(skh.doc(skh.db,'creatives',context.creativeId));if(snap.exists())c=normalizeCreative({id:snap.id,...snap.data()})}catch(e){toast('Creative could not be opened: '+e.message,'error')}}else{const saved=localStorage.getItem(storageKey(c));if(saved&&confirm('Continue editing the recovered draft?'))try{c=normalizeCreative(JSON.parse(saved))}catch(e){}}
 const entity=await fetchEntity(context);if(entity)c=applyEntity(c,entity,context.sourceType);if(!c.brandKit){try{const localBrand=localStorage.getItem('skh_brand_kit');if(localBrand)c.brandKit=JSON.parse(localBrand)}catch(e){}if(skh.currentUser)try{const bs=await skh.getDoc(skh.doc(skh.db,'brandKits',skh.currentUser.uid+'_default'));if(bs.exists())c.brandKit=bs.data()}catch(e){}}state=c;history=new CreativeHistory(c);selected='';m.classList.add('open');document.body.classList.add('skh-cs-open');render();setTimeout(()=>action('fit'),40)}
function close(){clearTimeout(saveTimer);if(state)saveDraft(false);$('#skhCreativeStudio')?.classList.remove('open');document.body.classList.remove('skh-cs-open')}
window.SokoHaiCreativeStudio={open,close,createCreative,validateCreative};window.CreativeEditor=window.SokoHaiCreativeStudio;window.openSokoHaiCreativeStudio=open;
// Natural integrations: preserve legacy editors for old records; all new design work uses this one Studio.
window.skhAdvertiseOpenEntity=function(){const p=skh.currentOpenProduct;if(!p)return toast('Open a Product or Service first.','error');const col=String(p.collectionName||p._collection||'products');const sourceType=col==='services'?'service':col==='delivery'||col==='drivers'?'transport':'product';open({sourceType,sourceId:p.id,format:'square',publicationType:'advertisement'});};
if(window.openAnnouncementFormModal&&!window.openLegacyAnnouncementFormModal){window.openLegacyAnnouncementFormModal=window.openAnnouncementFormModal;window.openAnnouncementFormModal=function(editId){const old=editId&&(window.__sokohaiAnnouncementsCache||[]).find(a=>a.id===editId);if(old&&!old.creativeId)return window.openLegacyAnnouncementFormModal(editId);return open(editId?{creativeId:old&&old.creativeId,publicationType:'advertisement'}:{format:'landscape',publicationType:'advertisement'});};}
window.skhOpenCompanyAdsSoon=function(){open({format:'square',publicationType:'advertisement'});};
